# nix-darwin build outputs
result

# editor/backup files
*.bak
*.bak[0-9]
*~
\#*\#

# sensitive / local-only
.DS_Store
.pi/

# npm-proxy build artifacts (regenerated by scripts/npm-proxy-build.sh;
# cert/key are local self-signed, log is per-run)
scripts/npm-proxy-cert.pem
scripts/npm-proxy-key.pem
scripts/npm-proxy.log

# Hermes session dump (accidental)
session.json
